// Stub — replaced with full implementation in Task 16
export default function SettingsScreen({ onBack }) {
  return (
    <div style={{ padding: '20px 16px' }}>
      <button onClick={onBack}>← Back</button>
      <p>Settings coming soon</p>
    </div>
  )
}
